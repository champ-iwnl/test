NEXT_PUBLIC_API_URL= http://backend:3001
#http://backend:3001
# API Settings
# timeout สำหรับ request (ms)
NEXT_PUBLIC_API_TIMEOUT=10000
# จำนวนครั้งที่ retry เมื่อ request ล้มเหลว
NEXT_PUBLIC_API_RETRY_COUNT=3
# ระยะเวลารอก่อน retry (ms)
NEXT_PUBLIC_API_RETRY_DELAY=1000

# App Settings (การตั้งค่าแอป)
NEXT_PUBLIC_APP_NAME="Spin Game"
NEXT_PUBLIC_APP_DESCRIPTION="หมุนวงล้อลุ้นรางวัล"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Home Page Settings (การตั้งค่าหน้า Home)
# จำนวนรายการประวัติต่อหน้า
NEXT_PUBLIC_HISTORY_PAGE_SIZE=10
# จำนวนรายการที่โหลดล่วงหน้า
NEXT_PUBLIC_PREFETCH_SIZE=5